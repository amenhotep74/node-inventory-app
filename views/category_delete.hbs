<h1>{{title}}</h1>
<p>Delete {{category.name}}</p>
{{!-- Show products that are in the category that was selected to delete --}}
{{#if category_products.length}}
<p>All products must be deleted before you delete this category!</p>
<div>
    <h4>Products still in category:</h4>
    <ul>
        {{#each category_products}}
        <li><a href="{{url}}">{{name}}</a></li>
        {{/each}}
    </ul>
</div>
{{else}}
<p>Do you really want to delete this category?</p>
<form method="POST" action="">
    <input type="hidden" name="categoryid" required="true" value="{{category.id}}">
    <button type="submit">Delete</button>
</form>
</div>
{{/if}}